meta:
  artifact_type: change_spec
  layer: L8

change:
  change_key: CHG-024
  description: Resolve playground 404 and enforce tenant/org/service scoped bot loading and querying; replace mock services with DB-backed service listing.
  affected_layers:
    - L0
    - L1
    - L2
    - L3
    - L4
    - L5
    - L6
    - L7
    - L8

requirements:
  functional:
    - Add bot routes for embed/iframe and widget loader.
    - Pass and use service_id, tenant_id, org_id for bot context.
    - Update playground and success pages with correct bot URLs and scoped embed code.
    - Replace mock services pages with API-backed data from DB.
  non_functional:
    - Keep service and bot creation wizard steps unchanged.
    - Keep tenant context separation explicit in URLs and request metadata.

required_tests:
  - Backend python syntax compile for changed API/chat files.
  - Container type checks attempted; existing unrelated repo-wide errors documented.
  - Route/path scan confirms new bot embed/widget endpoints and updated URLs.

success_criteria:
  - Playground no longer points to missing bot routes.
  - Embed code in "View All Embed Options" is updated and context-aware.
  - Services dashboard/list pulls live data from DB-backed APIs.
