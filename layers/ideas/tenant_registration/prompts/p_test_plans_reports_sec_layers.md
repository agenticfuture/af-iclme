- now looking at the implementation plan, let's move to the security layer of this cap...What will it entail now with the current code state and also you have to produce test plans and test reports  per each layer (cause we know that the tests you did so far where for the Product layer) and that now we are moving to the security layer... (So afterward you must produce the test plans and test reports for each layers ) (stored in ./af-iclme/layers/ideas/$(idea)/Product and 
./af-iclme/layers/ideas/$(idea)/Security) also the tracability report as well for each (only write there once they have all passed


- also add a security layer to ensure that the saas app can't be embedded into an iframe