## Prompt for the AI agent:
(Execution plan template run #46)

Primary outcomes in this run:
- Restore Prefect orchestrator decorators in chatbot request workflows (traceability retained).
- Keep required streaming architecture unchanged.
- Preserve smooth incremental streaming to UI while adding resilient fallback when Prefect is temporarily unavailable.
- Verify live stream behavior and flow creation after restart.

Tracking note:
- Implementation details and verification evidence are recorded in CHG-042 walkthrough.
