## Prompt for the AI agent:
(Execution plan template run #40)

Primary outcomes targeted in this run:
- Keep the previously delivered service lifecycle/state-machine/job-domain/status controls.
- Keep SaaS analytics/settings/users E2E improvements with `human Agent` role support.
- Keep create-service finish using true backend job execution state.
- Keep duplicate-name protection for services and bots.
- Revert chatbot DB storage target back to NeonDB for now (as requested).
- Recreate chatbot runtime and re-run migrations against NeonDB.

Tracking notes:
- Runtime verification and migration checks are documented in CHG-036 walkthrough.
